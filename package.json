{
  "name": "autofix-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Autonomous DevOps Incident Response Agent - 6 AI Agents working together",
  "dependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "start": "concurrently \"cd backend && node index.js\" \"cd frontend && npm start\"",
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend && echo \"All packages installed!\"",
    "build": "cd frontend && npm run build"
  }
}